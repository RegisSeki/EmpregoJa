<h1>Emprego Já</h1>

<% @jobs.each do |job| %>
  <h1><%= link_to job.title, job_path(job.id) %></h1>
  <p><%= job.category %></p>
  <p><%= job.company.name %></p>
  <p><%= job.location %></p>
  <% if  job.featured? %>
  <strong>Destaque</strong>
  <% end %>
<% end %>
